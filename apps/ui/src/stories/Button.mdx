import { Meta, Story, Canvas, Controls } from '@storybook/addon-docs/blocks';
import { Button } from '../components/ui/button';

<Meta title="UI Kit/Button" component={Button} />

# Button

The After6ix button component is built with our design tokens and provides consistent interaction patterns across the application.

## Variants

<div className="grid grid-cols-2 gap-4 my-8">
  <div className="space-y-4">
    <div>
      <Button variant="default">Primary Button</Button>
      <p className="text-sm text-neutral-600 mt-2">Default primary action</p>
    </div>
    
    <div>
      <Button variant="secondary">Secondary Button</Button>
      <p className="text-sm text-neutral-600 mt-2">Secondary actions</p>
    </div>
    
    <div>
      <Button variant="accent">Accent Button</Button>
      <p className="text-sm text-neutral-600 mt-2">Highlight important actions</p>
    </div>
    
    <div>
      <Button variant="destructive">Destructive Button</Button>
      <p className="text-sm text-neutral-600 mt-2">Dangerous actions like delete</p>
    </div>
  </div>
  
  <div className="space-y-4">
    <div>
      <Button variant="outline">Outline Button</Button>
      <p className="text-sm text-neutral-600 mt-2">Low emphasis actions</p>
    </div>
    
    <div>
      <Button variant="ghost">Ghost Button</Button>
      <p className="text-sm text-neutral-600 mt-2">Minimal style for tertiary actions</p>
    </div>
    
    <div>
      <Button variant="link">Link Button</Button>
      <p className="text-sm text-neutral-600 mt-2">Navigation or external links</p>
    </div>
  </div>
</div>

## Sizes

<div className="flex items-center gap-4 my-8">
  <Button size="sm">Small</Button>
  <Button size="default">Default</Button>
  <Button size="lg">Large</Button>
  <Button size="xl">Extra Large</Button>
  <Button size="icon">
    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" strokeWidth={1.5} stroke="currentColor" className="w-4 h-4">
      <path strokeLinecap="round" strokeLinejoin="round" d="M12 4.5v15m7.5-7.5h-15" />
    </svg>
  </Button>
</div>

## States

<div className="flex flex-col my-8 gap-4">
  <div className="flex items-center gap-4">
    <Button>Normal</Button>
    <Button disabled>Disabled</Button>
  </div>
  
  <div className="flex items-center gap-4">
    <Button>
      <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" strokeWidth={1.5} stroke="currentColor" className="w-4 h-4">
        <path strokeLinecap="round" strokeLinejoin="round" d="M3 16.5v2.25A2.25 2.25 0 005.25 21h13.5A2.25 2.25 0 0021 18.75V16.5M16.5 12L12 16.5m0 0L7.5 12m4.5 4.5V3" />
      </svg>
      <span>With Icon</span>
    </Button>
    
    <Button disabled>
      <svg className="animate-spin h-4 w-4" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
        <circle className="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" strokeWidth="4"></circle>
        <path className="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
      </svg>
      <span>Loading</span>
    </Button>
  </div>
</div>

## Usage Examples

### Basic Usage

```tsx
import { Button } from '@after6ix/ui/components/button';

function MyComponent() {
  return (
    <Button onClick={() => console.log('clicked')}>
      Click me
    </Button>
  );
}
```

### With Icons

```tsx
<Button>
  <DownloadIcon />
  Download Report
</Button>
```

### As Link

```tsx
<Button asChild>
  <a href="/dashboard">Go to Dashboard</a>
</Button>
```

### Button Group

<div className="inline-flex rounded-lg shadow-sm my-8">
  <Button variant="outline" className="rounded-r-none">Left</Button>
  <Button variant="outline" className="rounded-none border-l-0">Center</Button>
  <Button variant="outline" className="rounded-l-none border-l-0">Right</Button>
</div>

```tsx
<div className="inline-flex rounded-lg shadow-sm">
  <Button variant="outline" className="rounded-r-none">Left</Button>
  <Button variant="outline" className="rounded-none border-l-0">Center</Button>
  <Button variant="outline" className="rounded-l-none border-l-0">Right</Button>
</div>
```

## Accessibility

- All buttons have proper focus states with visible focus rings
- Disabled buttons have `aria-disabled` and prevent interaction
- Icon-only buttons should include proper aria-labels
- Keyboard navigation works with Tab and Enter/Space keys

## Design Tokens Used

- **Colors**: Primary, Secondary, Accent, Error (destructive), Neutral
- **Typography**: Font size and weight tokens
- **Spacing**: Padding uses spacing tokens
- **Shadows**: Shadow tokens for depth
- **Animations**: Transition duration tokens
- **Border Radius**: Radius tokens for corners